import React from "react";

const FormPreview = () => {
  return (
    <div
      style={{
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        height: "100vh",
      }}
    >
      FormPreview
    </div>
  );
};

export default FormPreview;
